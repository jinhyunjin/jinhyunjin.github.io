# 작품 데이터 관리 안내

홈페이지의 한국어·영문 작품 목록과 첫 화면 대표작은
`data/artworks.json`에서 함께 관리한다. `script.js`나 HTML에 작품 정보를
중복해 입력하지 않는다.

## 작품 추가

1. 웹 공개가 가능한 작품 이미지를 `assets/artworks/`에 추가한다.
2. `data/artworks.json`에 아래 형식의 객체를 추가한다.
3. 배열의 위에 있는 작품이 홈페이지에서도 먼저 표시된다.
4. 한국어와 영문 정보를 모두 입력한다.

```json
{
  "image": "example.jpg",
  "title": {
    "ko": "작품명",
    "en": "Artwork Title"
  },
  "year": "2026",
  "medium": {
    "ko": "한지에 신암채",
    "en": "New mineral pigments on hanji"
  },
  "size": "72.7 × 53 cm"
}
```

JSON은 주석을 지원하지 않으며, 마지막 항목 뒤에 쉼표를 넣으면 안 된다.

## 대표작 변경

첫 화면에 보여줄 작품 하나에만 다음 속성을 추가한다.

```json
"hero": true
```

기존 대표작에서는 `hero` 속성을 제거한다. `hero: true`가 없거나 두 개
이상이면 검증이 실패한다.

## 순서 변경·삭제

- 순서 변경: JSON 배열 안에서 작품 객체 전체를 옮긴다.
- 삭제: JSON의 작품 객체를 제거한 뒤, 더 이상 쓰지 않는 이미지만
  `assets/artworks/`에서 정리한다.
- 기본 화면은 앞의 작품 6개를 보여주며, `더 보기` 숫자는 전체 작품
  개수에 맞게 자동 계산된다.

## 검증

커밋하기 전에 다음 명령을 실행한다.

```bash
node scripts/validate-artworks.mjs
```

다음 항목을 자동으로 확인한다.

- JSON 문법
- 필수 항목과 한국어·영문 정보
- 중복 이미지 파일명
- 실제 이미지 파일 존재 여부
- 대표작이 정확히 하나인지

GitHub Pages 배포 과정에서도 같은 검증이 자동으로 실행된다.
